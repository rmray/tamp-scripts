{
  "name": "tamp-scripts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/scripts/*"
  ],
  "scripts": {
    "clean": "rimraf --glob packages/**/dist",
    "build": "pnpm clean && rollup -c",
    "watch": "rollup -c -w",
    "release": "lerna version --conventional-commits --yes && git push --follow-tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.3",
    "tm-utils": "workspace:*",
    "lerna": "^9.0.4",
    "rimraf": "^6.1.2",
    "rollup": "^4.57.1"
  }
}
